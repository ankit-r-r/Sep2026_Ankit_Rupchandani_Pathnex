Day - 11 Kubernetes with Helm, Advanced Terraform and GitHub CI/CD


Ansible - Install Helm on Server

- name: Install Helm
  hosts: all
  become: yes
  tasks: 
   - name: Download Helm
     get_url:
      url: https://get.helm.sh/helm-v3.7.0-linux-amd64.tar.gz
      dest: /tmp/helm.tar.gz

   - name: Extract Helm
     unarchieve:
       src: /tmp/helm.tar.gz
       dest: /usr/local/bin/
       remote_src: yes

   - name: Set Helm binary permissions
     file: 
      path: /usr/local/bin/helm
      mode: '0755'


## Terraform - Provision EC2 with Elastic IP

resource "aws_eip" "max-effort-eip" {
    instance = aws_instance.max-effort_ec2.id
}

resource "aws_instance" "max-effort_ec2" {
    ami = "ami-0abcd1234abcd1234"
    instance_type = "t3.medium" 
    
    tags = {
        Name = "Max-Effort"
    }
}


## Kubernetes - Helm Chart for Nginx

helm repo add stable https://charts.helm.sh/stable
helm install max-effort-nginx stable/nginx-ingress

Jenkind - Multi-Stage Pipeline

pipeline {
    agent any
    envirnoment {
        IMAGE_NAME = 'max-effort-web-app'
    }
    stages {
        stage('Checkout') {
            steps {
                script {
                    docker.build("${env.IMAGE_NAME}")
                }
            }
        }
        stage('Push Docker Image') {
            steps {
                script {
                    docker.withRegistry('https://docker.io', 'docker-credentials') {
                        docker.image("${env.IMAGE_NAME}").push('latest')
                    }
                }
            }
        }
        stage('Deploy to Kubernetes') {
            steps {
                script {
                    sh 'kubectl apply -f deployment.yaml'
                }
            }
        }
    }
}


## GitLab CI/CD - Build, Push and Deploy with Helm

stages:
 - build
 - push
 - deploy

build:
 stage: build
 script:
  - docker build -t max-effort-web-app .

push:
 stage: push
 script:
  - helm repo add stable https://charts.helm.sh/stable
  - helm install max-effort-nginx stable/nginx-ingress



Docker
# Expose & Port Mapping

FROM nginx 
EXPOSE 80

docker run -d -p 8080:80 nginx
